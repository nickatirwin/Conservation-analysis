content = """name: conservation
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - diamond
  - mafft
  - iqtree
  - ete3
  - biopython
  - numpy
  - scipy
  - pandas
  - matplotlib
  - seaborn
"""
with open("environment.yml", "w") as f:
    f.write(content)
print("done")
